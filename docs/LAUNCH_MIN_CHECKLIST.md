# 洞察织机（InsightLoom）最小发布清单

适用目标：先低成本上线 `insightloomapp.com`，保证可访问、可检索、可迭代。

> 状态更新 2026-09-18:站点已在腾讯 EdgeOne Pages 上线(免费托管 + 自动部署),
> 以下勾选项均附验证方式;域名相关项等待身份证实名 → 购买 → ICP 备案后解锁。

## 1) 上线前（30 分钟）

- [x] 品牌文案统一：页面标题、Logo、README、favicon 一致 —— 最后两处旧品牌残留(`curator.db` 文件名、`CURATOR_` 前缀)已于 2026-09-18 清理,前者自动迁移数据,后者保留兼容映射
- [ ] 访问入口确认：`insightloomapp.com` 可作为唯一主入口 —— ⏸ 等域名购买+备案
- [x] 基础页面可用：首页可打开、核心按钮可点击、无明显报错 —— EdgeOne 线上验证(0 console errors,hero/GIF/对比表渲染正常)
- [x] HTTPS 生效：浏览器地址栏显示安全连接 —— EdgeOne 边缘自带证书;自定义域名绑定后同样自动覆盖

## 2) 域名与 DNS

- [ ] 在域名服务商完成 `insightloomapp.com` 购买 —— ⏸ 需身份证(注册局实名模板),购物车已备(¥83/首年)
- [ ] 按托管平台要求添加 DNS 记录（常见为 `A` 或 `CNAME`）
- [ ] 配置 `www` 到主域名跳转（`www.insightloomapp.com` → `insightloomapp.com`）
- [ ] 等待 DNS 生效后复测（可用手机网络再测一次）

## 3) 部署（零预算优先）

- [x] 先用免费托管跑通 —— 腾讯 EdgeOne Makers(国内边缘直连,比 vercel/pages.dev 更贴合国内访问)
- [ ] 再绑定自定义域名 `insightloomapp.com` —— ⏸ 等 ICP 备案
- [x] 打开自动部署（推送代码后自动更新站点） —— 已两次端到端验证(push → 17s 重新部署上线)
- [x] 保留一个回退版本（上次稳定构建） —— EdgeOne「部署记录」保留全部历史版本可回滚
- [x] CI 自动回归 —— GitHub Actions 冒烟测试 8 项(全新环境验证通过,含旧前缀兼容自检)

## 4) 基础 SEO（最小可用）

- [x] 页面 `<title>` 含品牌词与价值主张 —— 「洞察织机 InsightLoom — 把收藏变成可用知识」
- [x] `meta description` 描述产品用途 —— 落地页 meta 齐全
- [x] favicon 正常显示 —— `/assets/` 品牌图标在线 200
- [x] 准备 `og:title`、`og:description`、`og:image` —— 已配置;真实抓取效果待正式域名后复验

## 5) 发布后 24 小时内

- [ ] 自测三轮：桌面 Chrome、手机浏览器、隐私模式 —— ⏳ 正式域名上线后执行
- [ ] 接入 51.la 统计（国内节点友好，替换原 Plausible/Umami 设想；一行 script + 统计 ID）—— ⏳ 等注册拿统计 ID
- [ ] 监控 404/500（平台日志）并修复首批问题
- [ ] 收集首批反馈（3~5 人）并记录到待办
- [ ] 只做高优先修复，不做大重构

## 6) 下一步建议（MVP 之后）

- ~~增加独立的 `about` 页面~~ → 已以「关于这个项目」区块形式落地(独立开发者叙事 + 执行力证据卡)
- 增加使用示例（1 分钟上手）→ demo.gif 已内嵌;可再补图文版
- 准备一个对外演示链接 —— 临时用 EdgeOne 预览链接(3 小时有效期可随时生成);正式待域名
- 增加 `privacy` 页面(若接统计/云服务则必备)
